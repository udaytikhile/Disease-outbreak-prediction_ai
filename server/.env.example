# Disease Outbreak Prediction AI — Server Environment Variables
# Copy this file to .env and fill in your values.

# Flask environment: development | production | testing
FLASK_ENV=development

# Server port
FLASK_PORT=5001

# Secret key for session signing (REQUIRED in production)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=default-dev-secret-key

# Database URL (defaults to SQLite for development)
# For production, use PostgreSQL: postgresql://user:pass@host:5432/dbname
# DATABASE_URL=sqlite:///outbreak.db

# CORS origins (comma-separated, REQUIRED in production)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Google Gemini API key for LLM-powered symptom checker
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=
